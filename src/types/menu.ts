export type SubmenuItem = {
  label: string;
  href: string;
};

export type HeaderItem = {
  label: string;
  href: string;
};

export type ReviewsCardProps = {
  rating: string;
  currentSlide: string;
  message: string;
  name: string;
  role: string;
  image: string;
}